export const initPaymentSheet = async () => {
  console.warn("Stripe initPaymentSheet is not supported on web.");
  return { error: null };
};

export const presentPaymentSheet = async () => {
  console.warn("Stripe presentPaymentSheet is not supported on web.");
  return { error: null };
};
